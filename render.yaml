services:
  - type: web
    name: ai-attendance-app
    runtime: python
    plan: free
    buildCommand: PIP_NO_CACHE_DIR=1 CMAKE_BUILD_PARALLEL_LEVEL=1 MAKEFLAGS=-j1 pip install --upgrade pip setuptools wheel && PIP_NO_CACHE_DIR=1 CMAKE_BUILD_PARALLEL_LEVEL=1 MAKEFLAGS=-j1 pip install -r requirements.txt
    startCommand: python db_init.py && gunicorn app:app
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
      - key: FLASK_ENV
        value: production
      - key: FLASK_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: ai-attendance-db
          property: connectionString

databases:
  - name: ai-attendance-db
    plan: free
